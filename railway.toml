[deploy]
startCommand = "STREAMLIT_SERVER_HEADLESS=true streamlit run ps_business_suite/app.py --server.headless=true --server.port=${PORT:-8501} --server.address=0.0.0.0 --server.fileWatcherType=none"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
